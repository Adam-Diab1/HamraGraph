# HamraGraph-
A Java-based navigation system that implements Dijkstra's algorithm using adjacency lists to compute shortest paths. Generates turn-by-turn directional instructions (left/right, distance-based) between locations for realistic route guidance.
